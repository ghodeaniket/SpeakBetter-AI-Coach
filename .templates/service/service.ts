export interface {{ServiceName}}Options {
  // Service options
}

export interface {{ServiceName}} {
  // Service methods
}

export const create{{ServiceName}} = (options: {{ServiceName}}Options): {{ServiceName}} => {
  // Implementation
  return {
    // Method implementations
  };
};
