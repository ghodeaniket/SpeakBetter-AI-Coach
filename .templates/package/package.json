{
  "name": "@speakbetter/{{name}}",
  "version": "0.1.0",
  "private": true,
  "main": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "build": "tsc",
    "test": "jest",
    "lint": "eslint 'src/**/*.{ts,tsx}'",
    "clean": "rm -rf dist node_modules"
  },
  "dependencies": {
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "jest": "^29.6.3",
    "@types/jest": "^29.5.0",
    "eslint": "^9.21.0"
  }
}
